#This should work if all models get required.
#Not sure if this is a good idea or not ... thinking ...
#Good very clean
#Bad   CouchDBLocation is not a class, though actually
#Rails would have converted it to CouchDbLocation so maybe it's ok
#but it is still confusing.

CouchDBLocation = 'http://127.0.0.1:5984/' #used in log_in_form(s)