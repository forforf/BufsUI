<div class="borg_header">
    Links from descendants:
</div>
<div>
  <% @link_borg_data.each do |link_data| %>
    <% link_data.each do |url, label| %>
      <a class="borg_link_text" target="_blank" href='<%= url %>')"><%= label %></a>
    <% end %>
  <% end %>
 </div>